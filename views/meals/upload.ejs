<div class="min-h-screen bg-gradient-to-b from-orange-50 to-teal-50 py-12 px-4">
   <div class="max-w-2xl mx-auto">
      <div class="text-center mb-8">
         <h1 class="text-4xl font-bold mb-2">
            <span class="text-orange-500">Analyser</span>
            <span class="text-teal-400">Votre Repas</span>
         </h1>
         <p class="text-gray-600">
            Téléchargez une photo de votre repas pour une analyse nutritionnelle
         </p>
      </div>

      <div class="bg-white rounded-3xl border-2 border-orange-300 p-8 shadow-lg">
         <form method="POST" action="/meals/analyze" id="uploadForm" enctype="multipart/form-data">
            <div class="mb-6">
               <div id="dropZone"
                  class="border-3 border-dashed border-teal-300 rounded-2xl p-12 text-center hover:border-orange-400 transition-colors cursor-pointer bg-gradient-to-br from-orange-50 to-teal-50">
                  <div id="previewContainer" class="hidden mb-4">
                     <img id="imagePreview" class="max-w-full h-64 mx-auto rounded-lg shadow-md object-cover" />
                  </div>

                  <div id="uploadPrompt">
                     <svg class="mx-auto h-16 w-16 text-teal-400 mb-4" fill="none" stroke="currentColor"
                        viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                           d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
                     </svg>
                     <p class="text-xl font-semibold text-gray-700 mb-2">
                        📸 Cliquez une image
                     </p>
                     <p class="text-gray-500">PNG, JPG, JPEG jusqu'à 10MB</p>
                  </div>

                  <input type="file" name="mealImage" id="mealImage" accept="image/*" class="hidden" required />
               </div>
            </div>

            <div class="mb-6">
               <label class="block text-gray-700 font-semibold mb-2">Nom du repas (optionnel)</label>
               <input type="text" name="mealName" placeholder="Ex: Déjeuner du midi"
                  class="w-full p-3 border-2 border-gray-200 rounded-lg focus:border-teal-400 focus:outline-none" />
            </div>

            <div class="mb-6">
               <label class="block text-gray-700 font-semibold mb-2">Notes additionnelles (optionnel)</label>
               <textarea name="notes" rows="3" placeholder="Ajoutez des détails sur votre repas..."
                  class="w-full p-3 border-2 border-gray-200 rounded-lg focus:border-teal-400 focus:outline-none"></textarea>
            </div>

            <button type="submit" id="submitBtn"
               class="w-full bg-gradient-to-r from-orange-500 to-orange-600 text-white p-4 rounded-lg font-bold text-lg hover:from-orange-600 hover:to-orange-700 transition-all transform hover:scale-105 shadow-lg disabled:opacity-50 disabled:cursor-not-allowed">
               🔍 Analyser avec l'IA
            </button>
         </form>

         <div class="mt-6 text-center">
            <a href="/dashboard" class="text-teal-500 hover:text-teal-600 font-semibold">
               ← Retour au tableau de bord
            </a>
         </div>
      </div>

      <div class="mt-8 bg-white rounded-2xl border border-orange-200 p-6">
         <h3 class="font-bold text-lg text-gray-800 mb-3">
            ℹ️ Comment ça marche ?
         </h3>
         <ul class="space-y-2 text-gray-600">
            <li class="flex items-start">
               <span class="text-orange-500 mr-2">1.</span>
               <span>Prenez une photo claire de votre repas</span>
            </li>
            <li class="flex items-start">
               <span class="text-orange-500 mr-2">2.</span>
               <span>Téléchargez l'image dans le formulaire</span>
            </li>
            <li class="flex items-start">
               <span class="text-orange-500 mr-2">3.</span>
               <span>Notre IA analysera les calories et nutriments</span>
            </li>
            <li class="flex items-start">
               <span class="text-orange-500 mr-2">4.</span>
               <span>Recevez des recommandations personnalisées</span>
            </li>
         </ul>
      </div>
   </div>
</div>

<script>
   const dropZone = document.getElementById('dropZone');
   const fileInput = document.getElementById('mealImage');
   const imagePreview = document.getElementById('imagePreview');
   const previewContainer = document.getElementById('previewContainer');
   const uploadPrompt = document.getElementById('uploadPrompt');
   const submitBtn = document.getElementById('submitBtn');

   dropZone.addEventListener('click', () => fileInput.click());

   fileInput.addEventListener('change', e => {
      if (e.target.files.length > 0) {
         handleFileSelect(e.target.files[0]);
      }
   });

   function handleFileSelect(file) {
      if (file && file.type.startsWith('image/')) {
         const reader = new FileReader();
         reader.onload = e => {
            imagePreview.src = e.target.result;
            previewContainer.classList.remove('hidden');
            uploadPrompt.classList.add('hidden');
            submitBtn.disabled = false;
         };
         reader.readAsDataURL(file);
      }
   }
</script>